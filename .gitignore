__pycache__/
*.pyc
.pytest_cache/
tests/_tmp/
build/
dist/
.venv_build/
.venv/
shapefiles.zip

# Secrets (NUNCA subir)
.streamlit/secrets.toml
secrets.toml

# Datos del cliente (repo PUBLICO — los datos viven en Supabase, no en git)
rectifiers_processed.json
*REP*.xlsx

# Históricos de otros contratistas: copia local auditable (el repo es público)
historicos/
