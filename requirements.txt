# Rangos amplios (sin pins exactos) para que Streamlit Cloud use sus wheels
# pre-compilados y NO intente compilar pyarrow/streamlit desde cero.
# Solo límites superiores en los majors que rompen (pandas 3, numpy 3, supabase 3).
# NOTA: no fijar streamlit ni pyarrow — la nube ya trae versiones funcionales.
openpyxl>=3.1
pandas>=2.0,<3
numpy>=1.26,<3
scipy>=1.12
pyproj>=3.6
shapely>=2.0
pyshp>=2.3
scikit-learn>=1.4
pillow>=10
plotly>=5.18
supabase>=2,<3

# google-generativeai (Fotos IA) queda FUERA a propósito: arrastra
# grpcio/protobuf/google-api-core que rompían el build. El código maneja su
# ausencia sin fallar. Para reactivar Fotos IA, añadir aquí una versión probada.
matplotlib>=3.6,<4

# Asistente de revisión (ia_revision.py). Opcional: el módulo maneja su
# ausencia y el flujo de corrección funciona a mano sin él.
anthropic>=0.40,<2
